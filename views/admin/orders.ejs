<%- include('../partials/header') %>

<main class="main-content">
    <div class="container">
        <h1 class="page-title">Manage Orders</h1>
        
        <% if (orders.length === 0) { %>
            <p>No orders found</p>
        <% } else { %>
            <table class="admin-table">
                <thead>
                    <tr>
                        <th>Order ID</th>
                        <th>Customer</th>
                        <th>Email</th>
                        <th>Items</th>
                        <th>Total</th>
                        <th>Payment</th>
                        <th>Status</th>
                        <th>Date</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% orders.forEach(order => { %>
                        <tr>
                            <td>#<%= order.id %></td>
                            <td><%= order.first_name %> <%= order.last_name %></td>
                            <td><%= order.email %></td>
                            <td><%= order.item_count %></td>
                            <td>₦<%= Number(order.total_amount).toLocaleString() %></td>
                            <td><span class="badge badge-<%= order.payment_status %>"><%= order.payment_status %></span></td>
                            <td>
                                <form action="/admin/orders/update-status/<%= order.id %>" method="POST" style="display: inline;">
                                    <select name="status" onchange="this.form.submit()" class="status-select">
                                        <option value="pending" <%= order.status === 'pending' ? 'selected' : '' %>>Pending</option>
                                        <option value="processing" <%= order.status === 'processing' ? 'selected' : '' %>>Processing</option>
                                        <option value="shipped" <%= order.status === 'shipped' ? 'selected' : '' %>>Shipped</option>
                                        <option value="delivered" <%= order.status === 'delivered' ? 'selected' : '' %>>Delivered</option>
                                        <option value="cancelled" <%= order.status === 'cancelled' ? 'selected' : '' %>>Cancelled</option>
                                    </select>
                                </form>
                            </td>
                            <td><%= new Date(order.created_at).toLocaleDateString() %></td>
                            <td>
                                <button onclick="viewOrderDetails(<%= order.id %>)" class="btn btn-sm btn-info">
                                    <i class="fas fa-eye"></i>
                                </button>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </div>
</main>

<script>
    function viewOrderDetails(orderId) {
        alert('Order details view - Order ID: ' + orderId);
        // You can implement a modal or redirect to order detail page
    }
</script>

<%- include('../partials/footer') %>